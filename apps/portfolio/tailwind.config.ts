// Tailwind CSS v4 uses CSS-first configuration.
// Theme customization is done in src/app/globals.css via @theme {}.
// This file exists to help VS Code / Cursor IDEs recognize the Tailwind v4 project.
import type { Config } from "tailwindcss";

const config: Config = {};
export default config;
